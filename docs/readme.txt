This Directory will once hold documentation. So far there is only an examplefile for the template override function.

To Activate it, put this file in an location outside of EXT:sk_calendar/ and specify the path via Typoscript. The file will alter the daynames to "hello" in the monthly view. You'll get the idea